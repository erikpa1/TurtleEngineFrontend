import React from "react";

import {TGui} from "@external/tgui";

export default function SceneTaskAnswerEditor({}) {
    return (
        <TGui.Card>
            <TGui.CardHeader>
                Hello
            </TGui.CardHeader>
            <TGui.CardContent>
                There
            </TGui.CardContent>
        </TGui.Card>
    )
}