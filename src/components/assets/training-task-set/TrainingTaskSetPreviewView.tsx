import React from "react";
import {TGui} from "@external/tgui";

export default function TrainingTaskSetPreviewView({asset, forceLang}) {

    const [language, setLanguage] = React.useState("en")

    return (
        <TGui.Stack>

            Here

        </TGui.Stack>
    )
}

